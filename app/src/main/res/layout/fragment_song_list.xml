<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout"
    android:orientation="vertical"
    android:background="@drawable/bground"
    android:paddingHorizontal="@dimen/spacing_4">

    <!-- Result count bar — shown only during an active search -->
    <TextView
        android:id="@+id/result_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="6dp"
        android:textSize="12sp"
        android:textColor="@color/green"
        android:visibility="gone"
        tools:text="5 songs found"
        tools:visibility="visible"/>

    <!-- Search in lyrics option — visible only during an active search -->
    <CheckBox
        android:id="@+id/check_search_lyrics"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:text="@string/search_in_lyrics"
        android:textColor="?attr/textColor"
        android:buttonTint="?attr/colorAccent"
        android:visibility="gone"
        tools:visibility="visible" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_song"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingBottom="80dp"
            android:clipToPadding="false"
            android:visibility="visible"/>

        <GridView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/grid_song"
            android:paddingBottom="80dp"
            android:clipToPadding="false"
            android:numColumns="auto_fit"
            android:verticalSpacing="@dimen/spacing_4"
            android:horizontalSpacing="@dimen/spacing_4"
            android:visibility="gone"/>

        <!-- Empty state — shown when search returns no results in any book -->
        <LinearLayout
            android:id="@+id/empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="&#128269;"
                android:textSize="48sp"/>

            <TextView
                android:id="@+id/empty_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textSize="16sp"
                android:textColor="?attr/textColor"
                android:gravity="center"
                android:text="@string/no_results"/>

        </LinearLayout>

    </FrameLayout>

    <!-- Cross-book results section — visible only when current book has no results
         but other books contain a match -->
    <TextView
        android:id="@+id/txt_other_books_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="4dp"
        android:text="@string/found_in_other_books"
        android:textSize="11sp"
        android:textAllCaps="true"
        android:letterSpacing="0.08"
        android:textColor="?attr/colorAccent"
        android:visibility="gone"
        tools:visibility="visible"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_other_books"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="80dp"
        android:clipToPadding="false"
        android:visibility="gone"
        tools:visibility="visible"
        tools:listitem="@layout/item_search_result"/>

</LinearLayout>